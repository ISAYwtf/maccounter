import { INITIAL_STATE, ProfileInitialState } from '@store/profile-store/profileInitialState'

const profileReducer = (state = INITIAL_STATE): ProfileInitialState => state

export default profileReducer
